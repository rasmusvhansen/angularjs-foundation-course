(function () {
  'use strict';  
  // 1. Create a module called myApp.core which exposes a version constant 'VERSION'
  // 2. Create a module called myApp.app which depends on myApp.core
  // 3. Create a run function on this module, which injects '$rootScope' and 'VERSION'
  // 4. Expose version on $rootScope and display it on the page
  // 5. Expose todays date on $rootScope and display it on the page
  // 6. Expose a function isBeforeNoon on $rootScope. Use ng-if in html to conditionally 
  // display some information if it is before noon.
  // 7. Run the tests along the way (karma start in this folder)
  // 8. Write additional tests as necessary

})();