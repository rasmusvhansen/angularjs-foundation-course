(function () {
  angular.module('collapse', [])
  
  .directive('collapse', function () {
    return {
      // TODO: create the directive so it listens to scope.collapsed and adds an .expanded class if it is false (use ng-class)
      // use transclusion to get content into the collapse div.
      // Use the tests for guidance   
    };
  });
})();